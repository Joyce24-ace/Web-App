export function initOrgChart() {
    const inputs = document.querySelectorAll(".org-name");

    inputs.forEach(input => {
        input.addEventListener("input", () => {
            // future save logic
        });
    });
}